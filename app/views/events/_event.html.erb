<a href="<%= event_path(event) %>" class="list-group-item list-group-item-action flex-column align-items-start m-1">
  <div class="d-flex w-100 justify-content-between">
    <h5 class="mb-1"><%= event.title %></h5>
    <p class="mb-1">Creator: <%= event.creator.name %></p>
    <small>Click for details.</small>
  </div>
</a>