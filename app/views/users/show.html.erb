<div class="jumbotron">
  <h1>User's Page</h1>
  <p>User Name: <%= @user.name %></p>
  <p>Email: <%= @user.email %></p>
  <h2>Events:</h2>
  <%= render 'events', object: @user.created_events %>
  <h2>Attended Events:</h2>
  <%= render 'events', object: @user.attended_events %>
</div>